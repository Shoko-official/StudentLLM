# Keep the supported speech-translation runtime on the current secure major.
# The legacy COMET scorer has an incompatible transformers<5 constraint and is
# intentionally not part of the default environment.
transformers>=5.10,<6
